spring:
  cassandra:
    contact-points: 127.0.0.1
    port: 9042
    keyspace-name: my_keyspace
    local-datacenter: datacenter1
  kafka:
    bootstrap-servers: localhost:29093
    consumer:
      group-id: audit-topic-group
      enable-auto-commit: true
topic-to-consume-message: audit-topic